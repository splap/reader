#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

if ! command -v mint >/dev/null 2>&1; then
  echo "mint not found. Install with: brew install mint" >&2
  exit 1
fi

mint bootstrap
mint run swiftformat . --config .swiftformat
