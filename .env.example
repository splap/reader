# Local environment settings (copy to .env and customize)

# Directory containing epub files to load into the simulator
# TEST_BOOKS_DIR=/path/to/your/books

# OpenRouter API key (deployed to simulator UserDefaults on each run)
# OPENROUTER_API_KEY=sk-or-v1-your-key-here
